#include "cardclass.h"
#include "fstream"
#include "sstream"

using namespace std;

Node::Node() {
}

Node::~Node() {

}

void Node::write(string s, int n) {
    stringstream ss;
    ss << s;
    for (int i = 0;ss >>str[i][n] && i < MAX; ++i) {

    }
    currentId = stoi(this->str[0][0]);
}